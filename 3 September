const updatedCards = (isMPST
  ? [...config?.section1?.cards, config?.section1?.card]
  : config?.section1?.cards
)?.map((card, index, arr) => ({
  ...card,
  isBottomDivider: index !== arr.length - 1, // true for all except the last one
}));
